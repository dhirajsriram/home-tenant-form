import React from 'react';
import Loader from '../common/loader/loader';

const Form: React.FC = () => {
  return (
    <div className="home-container">
      <Loader></Loader>
    </div>
  );
}

export default Form;
